package entity

/**
 * All possible poker hand categories.
 */
enum class ScoreTable {
    NONE,
    HIGHCARD,
    PAIR,
    TWOPAIR,
    SET,
    STRAIGHT,
    FLUSH,
    FULLHOUSE,
    FOUROFAKIND,
    STRAIGHTFLUSH,
    ROYALFLUSH
}